// Variables to use for naming/scripts
CONFIGURATION = Debug

// Build configuration
GCC_PREPROCESSOR_DEFINITIONS_CFG = _DEBUG DEBUG=1
GCC_OPTIMIZATION_LEVEL = 0
COPY_PHASE_STRIP = NO

// Include the "shared" default options last, so it can reference stuff from here
#include "defaults.xcconfig"
